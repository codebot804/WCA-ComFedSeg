# Configs

YAML files describing experiment defaults, method roles, and training-budget presets.

- `default.yaml`: minimal runner defaults.
- `method_registry.yaml`: approved baselines, WCA variants, and excluded diagnostic variants.
- `training_budget_registry.yaml`: smoke-test, pilot, and scoped multi-seed budgets.
